# Only needed if your contact API is hosted somewhere other than the default
# Vercel serverless function at /api/contact (e.g. the PHP backend/ folder).
# Copy this file to .env and fill in the URL.
VITE_CONTACT_ENDPOINT=/api/contact
